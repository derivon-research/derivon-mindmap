# PROTOTYPE · 大图的进入 / 定向 / 阅读

**一次性代码，不进 main，不要在它上面继续开发。** 它只回答一个问题：

> 打开一张 293 个概念的图，用户从哪里开始？

三处已知断裂：大图布局给不出入口；求解路线要求手动指定起点与目标，而用户两个输入都提供不了；把图当教材读时必须退出文档才能看下一步。

## 怎么跑

```sh
npm run prototype:entry
```

或 `npm run dev` 后打开 <http://localhost:1420/prototypes/entry-orientation/index.html>。

底部悬浮条切换变体，`←` / `→` 方向键同样可切。变体写在 URL 的 `?variant=` 上，可分享、刷新不丢。

## 四个变体

| key | 名字 | 主张 |
| --- | --- | --- |
| `0` | 现状（对照） | 今天的样子：293 点 force 布局 + 「请在图上点出起点和目标」。存在的意义是让另外三个有东西可比 |
| `A` | 搜索先行 | **图不是入口，目标才是。** 先问「你想学会什么」，再问「你已经会什么」，路线是一份可展开阅读的线性清单。大图默认从不出现 |
| `B` | 分区地图 | **图始终在，但从不以乱麻的形式出现。** 12 个领域块的概览图；已知集合按块批量勾选；路线按块分组；文档在右抽屉里读，图不消失 |
| `C` | 对话先行 | **图是对话的副产品。** Agent 问三个问题（为什么来 / 这几个你会哪些 / 确认），图被动点亮；出路线后直接进入一步一步的教学 |
| `D` | 融合稿（默认） | A/B/C 的取舍结果，**三个模式**而不是三块面板：**定向**（对话框兼搜索，保留预设问答，先收目标——**可以多个**——再一轮轮问已知；右侧大图随对话点亮）→ **学习**（教材占主体，**左栏 Agent 教学**接着定向态的对话位置，右栏路线子图记进度，大图退场）⇄ **浏览**（Obsidian 式自由逛，随手点开、设为目标或标记已会）。学习态两侧各有**展开 / 折叠 / 隐藏**三态，展开一侧自动收起另一侧；路线栏默认折叠成线性步骤清单，展开才画子图；Agent 的回答里出现整行公式或可交互组件时**自动展开** |

前四个在「从哪里开始」上根本分歧，不是配色差别：A 根本不画大图，B 把大图换成分区，C 把大图降级成状态显示。

`D` 是在这四个之上做出的取舍，保留的是：A 的即时资料（不必经 Agent 提问就能读文档）、C 的对话入口（比卡片更细地定位已知，可以多轮追问）、B 的路线子图（进度与剩余量）；否定的是：分区导航、大图作为操作对象、用卡片粒度问「你已经会什么」。大图不再常驻，而是一个**独立的浏览模式**，由按钮进入。

## 数据是真的，有两处不是

- **图**：`math-reforged` 的真实工作区，293 个概念 / 340 条推导，未删减。
- **文档**：真实的 `document.md`，含 KaTeX 公式与出处引用。
- **路线**：`generate-fixtures.mjs` 用**真实的 derivon CLI** 把「4 个已知集合预设 × 293 个目标」= 1172 条路线全部预先求解好（7.2 秒），所以浏览器里不需要求解器。这些是**精确解**（`provenOptimal`）。
- ⚠️ **自定义已知集合**（B 的按块勾选、C 的对话推断）不在预设里，用的是 `data.ts` 里一个粗糙的贪心近似，界面上标为「近似解」。它只是让原型点得动，不代表求解器的真实行为。
- ⚠️ **12 个领域块（tag）是手写的**，见 `generate-fixtures.mjs` 里的 `TAG_RULES`。真实的图**没有 tag 这个字段**。如果 B 赢了，「谁来打这些 tag、要不要进协议」就是一个必须回答的新问题。

重新生成 fixtures（需要 `derivon` CLI 和同级的 `../math-reforged` 克隆）：

```sh
node prototypes/entry-orientation/generate-fixtures.mjs
```

## 不在范围内

- 编辑（建点、连边、替换视图）：这里只有读者视角。
- 教学状态的持久化与回传：另一张票（教学回路与回传动机）。
- 性能：force 布局在这里是同步跑 300 tick 的，真实产品不能这么干。
